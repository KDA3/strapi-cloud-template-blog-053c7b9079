'use strict';

/**
 * scheduled-game controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::scheduled-game.scheduled-game');
